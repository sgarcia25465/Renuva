---
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/png" href="/newfavicon.png?v=3" />
		<link rel="shortcut icon" href="/newfavicon.png?v=3" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap"
			rel="stylesheet"
		/>
		<title>Gallery - Renuva</title>
		<style>
			:root {
				--very-dark-brown: #262D26;
				--dark-brown: #5E635D;
				--brown: #8D7C6E;
				--light-brown: #ADB69F;
				--cream: #F5F4EF;
				--white: #FFFFFF;
			}

			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			body {
				font-family: "Inter", "Segoe UI", sans-serif;
				background: linear-gradient(180deg, #ffffff, var(--cream));
				color: var(--very-dark-brown);
			}

			html, body {
				overflow-x: hidden;
			}

			header {
				position: sticky;
				top: 0;
				left: 0;
				width: 100%;
				z-index: 1000;
				background: rgba(250, 249, 246, 0.96);
				backdrop-filter: blur(10px);
				-webkit-backdrop-filter: blur(10px);
				padding: 1.05rem 0;
				border-bottom: none;
				box-shadow: none;
			}

			.header-content {
				display: flex;
				justify-content: space-between;
				align-items: center;
				padding: 0 2rem 0 clamp(2.2rem, 8vw, 8rem);
			}

			.logo {
				height: 60px;
				width: 200px;
				overflow: visible;
				display: flex;
				align-items: center;
				justify-content: flex-start;
			}

			.logo img {
				height: 100%;
				width: auto;
				display: block;
				transform: scale(0.70);
				transform-origin: left center;
			}

			nav {
				display: flex;
				align-items: center;
				gap: 0.25rem;
			}

			nav a {
				text-decoration: none;
				color: var(--dark-brown);
				font-family: "Playfair Display", serif;
				font-weight: 500;
				font-size: 0.95rem;
				letter-spacing: 0.02em;
				padding: 0.42rem 0.85rem;
				border-radius: 8px;
				transition: color 0.3s, background 0.3s;
			}

			nav a:hover {
				color: var(--brown);
				background: rgba(141, 124, 110, 0.07);
			}

			nav .nav-cta {
				background: var(--brown);
				color: var(--white);
				padding: 0.46rem 1.1rem;
				border-radius: 50px;
				font-family: "Inter", sans-serif;
				font-weight: 600;
				font-size: 0.72rem;
				letter-spacing: 0.06em;
				text-transform: uppercase;
				transition: background 0.3s, transform 0.25s;
			}

			nav .nav-cta:hover {
				background: var(--very-dark-brown);
				transform: translateY(-1px);
			}

			main {
				max-width: 1120px;
				margin: 0 auto;
				padding: 3.5rem 1.5rem 4rem;
			}

			.hero {
				margin-bottom: 1.8rem;
			}

			.kicker {
				display: inline-block;
				font-size: 0.76rem;
				font-weight: 600;
				letter-spacing: 0.18em;
				text-transform: uppercase;
				color: var(--light-brown);
				margin-bottom: 0.7rem;
			}

			h1 {
				font-family: "Playfair Display", Georgia, serif;
				font-size: clamp(2.1rem, 5vw, 3.6rem);
				line-height: 1.08;
				color: var(--brown);
				margin-bottom: 0.7rem;
			}

			.hero p {
				max-width: 62ch;
				color: var(--dark-brown);
				line-height: 1.7;
			}

			.gallery-grid {
				display: grid;
				grid-template-columns: repeat(3, minmax(0, 1fr));
				gap: 1rem;
			}

			.gallery-slot {
				background: var(--white);
				border: 1px solid rgba(173, 182, 159, 0.45);
				border-radius: 16px;
				overflow: hidden;
				box-shadow: 0 10px 22px rgba(38, 45, 38, 0.07);
			}

			.slot-preview {
				aspect-ratio: 4 / 3;
				background:
					linear-gradient(135deg, rgba(215, 193, 184, 0.45), rgba(173, 182, 159, 0.35)),
					repeating-linear-gradient(
						-45deg,
						rgba(255, 255, 255, 0.35) 0 14px,
						rgba(255, 255, 255, 0.1) 14px 28px
					);
				display: grid;
				place-items: center;
				color: rgba(38, 45, 38, 0.55);
				font-size: 0.9rem;
				font-weight: 600;
				letter-spacing: 0.06em;
				text-transform: uppercase;
			}

			.slot-caption {
				padding: 0.8rem 0.9rem 1rem;
				font-size: 0.92rem;
				color: var(--dark-brown);
			}

			@media (max-width: 900px) {
				.gallery-grid {
					grid-template-columns: repeat(2, minmax(0, 1fr));
				}
			}

			@media (max-width: 768px) {
				header { padding: 0.65rem 0; }
				.header-content {
					flex-direction: row;
					gap: 0.55rem;
					align-items: center;
					padding: 0 1rem;
				}
				.logo {
					height: 48px;
					width: 165px;
					overflow: visible;
					display: flex;
					align-items: center;
					justify-content: flex-start;
					margin: 0;
					flex: 0 0 auto;
				}
				.logo img {
					transform-origin: left center;
					transform: scale(0.70);
				}
				nav {
					width: auto;
					justify-content: flex-start;
					flex-wrap: nowrap;
					overflow-x: auto;
					-webkit-overflow-scrolling: touch;
					scrollbar-width: none;
					flex: 1 1 auto;
				}
				nav::-webkit-scrollbar { display: none; }
				nav a {
					white-space: nowrap;
					font-size: 0.78rem;
					padding: 0.3rem 0.5rem;
				}
				nav .nav-cta {
					margin-left: 0.2rem;
					padding: 0.34rem 0.7rem;
					font-size: 0.64rem;
				}
			}

			@media (max-width: 600px) {
				main {
					padding-top: 2.4rem;
				}

				.gallery-grid {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<header>
			<div class="header-content">
				<a href="/" style="text-decoration: none;">
					<div class="logo">
						<img src="/assets/images/RENUVAMAIN.png" alt="Renuva" />
					</div>
				</a>
				<nav>
					<a href="/about">About Us</a>
					<a href="/distributor">Distributor</a>
					<a href="/gallery">Gallery</a>
					<a href="/contact" class="nav-cta">Contact Us</a>
				</nav>
			</div>
		</header>

		<main>
			<section class="hero">
				<span class="kicker">Renuva</span>
				<h1>Gallery</h1>
			</section>

			<section class="gallery-grid" aria-label="Gallery placeholders">
				<article class="gallery-slot">
					<div class="slot-preview">Coming Soon</div>
					<p class="slot-caption">Featured Project Slot</p>
				</article>
				<article class="gallery-slot">
					<div class="slot-preview">Coming Soon</div>
					<p class="slot-caption">Kitchen Renewal Slot</p>
				</article>
				<article class="gallery-slot">
					<div class="slot-preview">Coming Soon</div>
					<p class="slot-caption">Commercial Space Slot</p>
				</article>
				<article class="gallery-slot">
					<div class="slot-preview">Coming Soon</div>
					<p class="slot-caption">Hospitality Project Slot</p>
				</article>
				<article class="gallery-slot">
					<div class="slot-preview">Coming Soon</div>
					<p class="slot-caption">Custom Wrap Detail Slot</p>
				</article>
				<article class="gallery-slot">
					<div class="slot-preview">Coming Soon</div>
					<p class="slot-caption">Before / After Slot</p>
				</article>
			</section>
		</main>
	</body>
</html>
